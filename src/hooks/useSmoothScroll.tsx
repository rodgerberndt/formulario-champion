/**
 * Smooth scroll hook — now uses native CSS scroll-behavior.
 * Lenis has been removed for better compatibility and performance.
 */
export function useSmoothScroll() {
  // Native smooth scroll is handled by CSS `scroll-behavior: smooth` in index.css.
  // This hook is kept as a no-op to avoid breaking imports.
}
